package app.view

import app.helper.${name}Helper
import org.jbrat.core.data.abstracts.Bindable
import org.jbrat.views.JBratView

@Mixin(${name}Helper)
class ${name}View extends JBratView{${note}
    @Override
    void render(Bindable bean) {

    }
}